project('LoRa', 'c', 'cpp',
  version: '0.1.0',
  default_options: [
    'cpp_std=c++17',
    'warning_level=3',   # replaces -Wall
    'optimization=0'     # replaces -O0
  ])

subdir('toolchain')
subdir('board')
subdir('libs')
subdir('app')

